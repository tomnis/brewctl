# Shared by every mode. Keep this minimal -- it is installed on the Pi Zero 2 W
# (416 MB RAM) as well as in the api container.
# Mode-specific deps live in hardware.txt / api.txt; test deps in dev.txt.
fastapi[standard]==0.128.0
fastapi-cli==0.0.20
