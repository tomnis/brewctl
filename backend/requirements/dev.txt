# Local development and CI.
#
# Deliberately does NOT include hardware.txt: bluepy and RPi.GPIO are Linux/Pi
# only and will not build on a dev machine. The device libraries are imported
# lazily inside create_scale()/create_valve() behind BREWCTL_IS_PROD, and the
# test suite uses mocks, so they are never needed off the Pi.
-r api.txt

pytest==9.0.2
# Shuffles test order every run (seed in the banner, repro via --randomly-seed).
# Test isolation here depends on per-test fixtures, not execution order -- this
# is what keeps that true. A failure that only shows up sometimes means an
# order dependency: rerun with the printed seed to reproduce.
pytest-randomly==4.1.0
